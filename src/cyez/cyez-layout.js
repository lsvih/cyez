class Layout {
    /**
     * 定义布局列表
     * @type {String[]}
     * @property
     * @public
     */
    layouts = []

    constructor() {
        const builtin_layouts = ['grid', 'circle', 'concentric', 'breadthfirst', 'cose']
        const plugin_layouts = ['cola', 'dagre', 'elk', 'klay', 'spread']



    }
}

export default Layout